export interface UserLocation {
  lat: number;
  lon: number;
  city?: string;
  country?: string;
  // name: string;
  // country: string;
  // state: string;
  // region: string;
  // timezone_id: string;
  // localtime: string;
  // localtime_epoch: number;
  // utc_offset: string;
}

